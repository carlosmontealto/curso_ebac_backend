server:
  port: 8081
  wait-time-in-ms-when-sync-empty: 3000

spring:
  spring:
    profiles:
      active: native
  data:
    mongodb:
      uri: mongodb://admin:admin@127.0.0.1:28017/
      database: client
      auto-index-creation: true

application-version: 1.0
